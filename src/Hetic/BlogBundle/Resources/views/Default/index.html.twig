Hello {{ name }}!
{% for flashMessage in app.session.flashbag.get('notice') %}
    <div class="flash-notice">
        {{ flashMessage }}
    </div>
{% endfor %}

{{ render(controller("HeticBlogBundle:Default:response")) }}

<pre>
    {{ dump(name) }}
</pre>