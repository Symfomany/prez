language: php

php:
#  - 5.3
  - 5.4

before_script:
  - curl -s http://getcomposer.org/installer | php --
  - php composer.phar install --dev
  - mysql -p -h 127.0.0.1  -u root -ppass localhost symfony < mysql/symfony.sql

script: phpunit -c app/

notifications:
  email:
    - julien@eetserious.com
